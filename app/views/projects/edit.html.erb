<%= render "shared/remote_modal", title: "Edit this Project" do %>

<%= render "form", project: @project %>
<%= render 'shared/errors', obj: @project %>
<br>

<div>
  <%= link_to "Show this project", @project %> |
  <%= link_to "Back to project", project_path(@project) %>
</div>
<% end %>